@model IEnumerable<Ogame.Models.TemporalAction>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Due_to)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Type)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Target)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Due_to)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Type)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Target.PlanetID)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.TemporalActionID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.TemporalActionID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.TemporalActionID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
